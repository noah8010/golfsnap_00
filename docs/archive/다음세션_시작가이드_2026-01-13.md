# 다음 세션 시작 가이드 - 2026-01-13

> **이전 세션**: 2026-01-13 오후  
> **완성도**: 85% (유지)  
> **상태**: 버그 2개 미해결

---

## 🚨 **즉시 해결 필요한 버그**

### **1. scrollLeft 초기화 문제** 🔴
```
현상:
- useEffect로 scrollLeft = 164.5 설정
- 하지만 패널 열 때 scrollLeft = 0
- playheadTime이 항상 0으로 계산됨
- 텍스트/오디오/필터가 항상 0초에 생성됨

로그:
[Timeline] 초기 스크롤 위치 설정: 164.5
[TextClip] currentScrollLeft: 0 ❌

위치:
- src/screens/EditorWorkspaceScreen.tsx
- useEffect (라인 ~440)
- handleAddText, handleApplyAudio, handleApplyFilter
```

### **2. 범위 제한 미작동** 🔴
```
현상:
- 텍스트/오디오가 비디오 55초 범위 벗어남
- 코드는 작성되어 있으나 실행 안됨

원인:
- scrollLeft = 0 문제와 연관
- useEffect 실시간 검증이 작동 안함

위치:
- src/hooks/useTimeline.ts
- useEffect (라인 ~38-80)
- moveClip, trimClipStart, trimClipEnd
```

---

## 🔧 **해결 방향**

### **Option 1: useLayoutEffect 사용**
```typescript
// useEffect 대신
React.useLayoutEffect(() => {
  if (timelineRef.current && leftPadding > 0) {
    timelineRef.current.scrollLeft = leftPadding;
    setScrollOffset(leftPadding);
  }
}, [leftPadding, timelineZoom]);
```

### **Option 2: 초기화 완료 플래그**
```typescript
const [isScrollInitialized, setIsScrollInitialized] = useState(false);

useEffect(() => {
  if (!isScrollInitialized && timelineRef.current && leftPadding > 0) {
    timelineRef.current.scrollLeft = leftPadding;
    setScrollOffset(leftPadding);
    setIsScrollInitialized(true);
  }
}, [leftPadding, isScrollInitialized]);
```

### **Option 3: scrollOffset를 단일 진실 공급원으로**
```typescript
// scrollLeft는 scrollOffset의 mirror로만 사용
useEffect(() => {
  if (timelineRef.current) {
    timelineRef.current.scrollLeft = scrollOffset;
  }
}, [scrollOffset]);

// 모든 계산은 scrollOffset state 사용
const playheadTime = (scrollOffset + centerOffset - leftPadding) / ...;
```

---

## 📋 **세션 시작 체크리스트**

### **1단계: 현황 파악 (10분)**
- [ ] 이 문서 읽기
- [ ] docs/세션요약_2026-01-13_오후.md 읽기
- [ ] 브라우저 완전 새로고침 (Ctrl + Shift + R)
- [ ] F12 콘솔 열기
- [ ] 텍스트 추가 테스트
- [ ] 로그 확인

### **2단계: scrollLeft 문제 해결 (1시간)**
- [ ] Option 1 시도: useLayoutEffect
- [ ] 테스트 및 로그 확인
- [ ] 실패 시 Option 2 시도
- [ ] 테스트 및 로그 확인
- [ ] 실패 시 Option 3 시도

### **3단계: 범위 제한 검증 (1시간)**
- [ ] scrollLeft 해결 후 자동으로 해결되는지 확인
- [ ] 안되면 useEffect 로직 재검증
- [ ] 의존성 배열 재확인
- [ ] 실제 범위 제한 테스트

### **4단계: 클린업 (30분)**
- [ ] 디버깅 로그 제거 또는 개발 모드만
- [ ] 임시 문서 8개 검토 및 정리
- [ ] 문서 업데이트
- [ ] 린트 체크

---

## 🔍 **디버깅 팁**

### **scrollLeft 추적**
```javascript
// 콘솔에서 직접 실행
setInterval(() => {
  const el = document.querySelector('.overflow-x-auto');
  console.log('scrollLeft:', el?.scrollLeft);
}, 1000);
```

### **React DevTools**
```
1. Components 탭
2. EditorWorkspaceScreen 선택
3. Hooks 섹션에서 scrollOffset 값 확인
4. timelineRef.current.scrollLeft와 비교
```

### **브레이크포인트**
```
1. EditorWorkspaceScreen.tsx:440 (초기 스크롤 설정)
2. EditorWorkspaceScreen.tsx:374 (텍스트 추가 시 scrollLeft 읽기)
3. 두 지점에서 scrollLeft 값 비교
```

---

## 📚 **관련 파일**

### **수정 필요**
```
🔴 src/screens/EditorWorkspaceScreen.tsx
   - scrollLeft 초기화
   - playheadTime 계산
   
🔴 src/hooks/useTimeline.ts
   - useEffect 범위 검증
   - 의존성 배열
```

### **정상 작동**
```
✅ src/hooks/useDragClip.ts
✅ src/components/TrimHandle.tsx
✅ src/components/TimelineClip.tsx
✅ src/index.css
```

---

## 💡 **코드 힌트**

### **현재 문제 코드**
```typescript
// EditorWorkspaceScreen.tsx
useEffect(() => {
  if (timelineRef.current && leftPadding > 0) {
    timelineRef.current.scrollLeft = leftPadding; // 설정됨
    setScrollOffset(leftPadding); // 설정됨
  }
}, [leftPadding, timelineZoom]);

// 이후
const currentScrollLeft = timelineRef.current.scrollLeft; // 0 ❌
```

### **의심되는 부분**
```typescript
// 1. React Strict Mode?
// 2. 다른 useEffect가 scrollLeft를 0으로 설정?
// 3. Hot Module Replacement 부작용?
// 4. onScroll 이벤트가 scrollLeft를 리셋?
```

---

**작성**: 2026-01-13  
**다음 세션**: scrollLeft 문제 해결부터 시작  
**예상 시간**: 2-3시간
